/*
 * Тут должен быть первый запрос вызывающий информацию о пользователе,
 * основываясь на токене которая будет приходить из localstorage.
 * Ключ токена 'ACCESS_KEY', на локальной машине он должен вставляться вручную
 */
