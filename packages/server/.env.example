# =====================================================
# BANKIMONLINE ENVIRONMENT VARIABLES TEMPLATE
# =====================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual database credentials
# 3. For local development, set up PostgreSQL or use Docker
# =====================================================

# =====================================================
# DATABASE CONFIGURATION (REQUIRED)
# =====================================================

# Main Database Connection - Core application data
DATABASE_URL=postgresql://username:password@localhost:5432/bankimonline_main

# Content Database Connection - Translation and content management
CONTENT_DATABASE_URL=postgresql://username:password@localhost:5432/bankimonline_content

# =====================================================
# SERVER CONFIGURATION
# =====================================================

# Server Port (default: 8003)
PORT=8003

# Node Environment (development/production)
NODE_ENV=development

# JWT Secret for authentication (generate a secure random string)
JWT_SECRET=your-super-secret-jwt-key-here

# JWT Token expiration time
JWT_EXPIRES_IN=7d

# =====================================================
# CORS CONFIGURATION
# =====================================================

# Allowed CORS origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# =====================================================
# LOCAL DEVELOPMENT SETUP OPTIONS
# =====================================================

# Option 1: Docker PostgreSQL (recommended for local dev)
# docker run --name bankimonline-db -e POSTGRES_PASSWORD=yourpassword -p 5432:5432 -d postgres:15
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/postgres
# CONTENT_DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/postgres

# Option 2: Railway Development Database (if you have access)
# Contact team lead for development database credentials

# Option 3: Local PostgreSQL Installation
# Install PostgreSQL locally and create databases:
# createdb bankimonline_main
# createdb bankimonline_content

# =====================================================
# OPTIONAL CONFIGURATIONS
# =====================================================

# Email Configuration (for notifications - optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# SMS Configuration (for authentication - optional)
# SMS_API_KEY=your-sms-provider-key
# SMS_FROM=+1234567890

# File Upload Configuration
# UPLOAD_MAX_SIZE=10485760
# UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,application/pdf

# =====================================================
# RAILWAY DEPLOYMENT (Production Only)
# =====================================================

# These are set automatically by Railway - do not modify
# RAILWAY_ENVIRONMENT=production
# RAILWAY_PROJECT_ID=your-project-id
# RAILWAY_SERVICE_ID=your-service-id

# =====================================================
# DEVELOPMENT NOTES
# =====================================================

# 🔐 Security Best Practices:
# 1. NEVER commit .env files to git
# 2. Use strong, unique passwords for databases
# 3. Generate secure JWT secrets: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Use different credentials for each environment

# 📦 Database Setup:
# 1. Run database migrations: npm run migrate
# 2. Seed initial data: npm run seed
# 3. Verify connection: npm run test:db

# 🚀 Quick Start:
# 1. Copy this file: cp .env.example .env
# 2. Update DATABASE_URL and CONTENT_DATABASE_URL
# 3. Generate JWT_SECRET: openssl rand -hex 32
# 4. Start development: npm run dev